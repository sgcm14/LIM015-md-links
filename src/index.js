const {
    tobeAbsolute,
    resolvePathA,
    pathExists,
    findDirectory,
    readFileAndDirectory,
    extractTheLinks,
    confirmOptions,
  } = require('./api');

const mdLinks = (path, options = {}) => {
    return new Promise((resolve, reject) => {
        
    })
}